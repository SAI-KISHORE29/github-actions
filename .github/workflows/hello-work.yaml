name: Hello world
on: 
  push:
    branches:
      - develop
run-name: workflow from develop branch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world action
        run: echo "Hello world!"